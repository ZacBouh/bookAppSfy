{% extends 'template.html.twig' %}

{% block PageTitle %}
    Livres en catalogue
{% endblock %}

{% block PageContent %}
<div class="container-fluid justify-content-center">

    <div class="row ">

    {% for item in items %}
    <div class="col-3 pt-2 pb-2">
    {{ include(cardTemplate) }}
    </div>
    {% endfor %}
    </div>
    {% if totalPages > 1 %}
        
    {{ include('Fragments/paginationNav.html.twig') }}
    {% endif %}

</div>
{% endblock %}